<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".confirmExportProduct.ConfirmExportProductActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/confirm_export_product_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/confirm_export_product_bottom_view"/>

    <LinearLayout
        android:id="@+id/confirm_export_product_bottom_view"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:gravity="center"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/confirm_export_product_export_button"
            android:layout_width="wrap_content"
            android:layout_height="50dp"
            android:layout_weight="1"
            android:backgroundTint="@color/Red_status"
            android:textColor="@color/white"
            android:text="@string/confirm_export_product_title"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>